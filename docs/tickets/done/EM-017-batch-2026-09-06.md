---
id: EM-017
title: Batch — trivial changes, 2026-09-06
status: done
tier: trivial
complexity: S
dependencies: []
claimed_by: claude-fable-5-1
claimed_at: 2026-09-06
closed_at: 2026-09-06
---

# EM-017 — Batch: trivial changes, 2026-09-06

## Context

The first batch ticket under `docs/ticket-lifecycle.md`, "Batching trivial
work". It collects trivial changes that arose from the assessment of this
repository at 0947dda and from the wave EM-006 to EM-014-001, none of which
warrants a ticket of its own.

## Specification

Entries are listed below as they are made, one per entry, each with its
tier, the answer that supports it, and its evidence. The batch closes on
the cap in the lifecycle section — ten entries or seven days from
`claimed_at`, whichever comes first — or earlier at the holder's choice,
which this one takes: the holder closes it before claiming other work.

### Public surface

N/A — this repository publishes documents. No rule changes; two closed
records gain a stated decision, two documents gain a corrected reference or
a dated note.

### Entries

*(listed in the commit that makes each)*

1. **EM-007's closed record states that the maintainer accepted the
   departure it recorded.** Its description had left the original Behaviour
   sentence open for reinstatement. Tier `trivial`: no clause of the operative test holds, and nothing a
   program executes or a caller reads as a contract is touched — a closed ticket
   file. Evidence: the line after "Departure from Behaviour, recorded" in
   `docs/tickets/done/EM-007-review-terminates-a-stopping-rule-for-independent-review.md`.

2. **EM-014-001's closed record states that the maintainer accepted the
   departure it recorded.** Tier `trivial`: no clause of the operative test holds, and nothing a
   program executes or a caller reads as a contract is touched — a closed ticket file.
   Evidence: the line after "Departure from Behaviour, recorded" in
   `docs/tickets/done/EM-014-001-both-columns-on-one-rule-is-a-class-signal.md`.

3. **The lifecycle's directory sketch named a `TEMPLATE.md` that does not
   exist here.** The template is `templates/TICKET.md`, and what the
   directory actually holds beside the four state directories is the board.
   Tier `trivial`: no clause of the operative test holds, and nothing a
   program executes or a caller reads as a contract is touched — one line of a diagram. Evidence: the sketch under
   the opening paragraph of `docs/ticket-lifecycle.md`.

4. **ADR-0001 and ADR-0002 record that they were created `accepted`.**
   The decision-record process says a status begins `proposed`; the first
   record to follow it is ADR-0003. Both are annotated rather than edited,
   for the reason ADR-0001 gives about backdating. Tier `trivial`: no clause of the operative test holds, and nothing a
   program executes or a caller reads as a contract is touched — a
   dated annotation on two records, append-only. Evidence: the annotation
   at the end of each file.

## Acceptance criteria

The entries are the criteria, each ticked with the commit that made it.

1. [x] AC1: EM-007's closed record states that its recorded departure was
   accepted — `4b90b09`.
2. [x] AC2: EM-014-001's closed record states the same — `ba12003`.
3. [x] AC3: the lifecycle's directory sketch names the board rather than a
   `TEMPLATE.md` that is not there — `826c524`.
4. [x] AC4: ADR-0001 and ADR-0002 carry a dated annotation recording that
   they were created `accepted` — `f305b94`.
5. [x] AC5: every entry is `trivial` by the operative test, and each entry
   above states the answer that supports it.

## Out of scope

- Any change that is not `trivial` by the operative test. A change found to
  be above `trivial` leaves this batch with a recorded finding.

## References

- `docs/ticket-lifecycle.md`, "Batching trivial work" — the rule this
  ticket is the first instance of.

## PR Description

### Ticket
EM-017 — Batch: trivial changes, 2026-09-06

### Tier
`trivial` — every entry is `trivial` by the operative test, each stating
the answer that supports it. Self-merged, per the tier table. This is the
first batch under `docs/ticket-lifecycle.md`, "Batching trivial work".

### Summary
Four trivial changes against one ticket: two closed records state that the
maintainer accepted the departures they had left open, the lifecycle's
directory sketch names the board rather than a file that is not there, and
the two founding decision records carry a dated note about their status.

### Acceptance criteria
The five above, all met; each entry names its commit.

### Falsification
N/A — no behavioural claim. Per entry, what a reader does differently:
- Entry 1: a reader of EM-007's record knows the cap's reasoning stands as
  landed, rather than awaiting a decision.
- Entry 2: a reader of EM-014-001's record knows the same of its
  description of the per-round record.
- Entry 3: a reader following the lifecycle's sketch looks for the template
  where it is.
- Entry 4: a reader comparing the decision records against the process's
  own status rule finds the discrepancy already recorded, and why the
  records were not edited to hide it.

### Out of scope (per ticket)
Confirmed: no entry was found above `trivial`, so none left the batch and
no reclassification finding is recorded. No rule or procedure changes; the
lifecycle edit is one line of a diagram, and both annotations are
append-only below a horizontal rule with each record's status unchanged.

### How to verify
1. `git log --oneline bcdfcfb..HEAD` — one claim commit, four entry
   commits, one close commit.
2. `git diff bcdfcfb..HEAD -- docs/adr/` — additions only, below a rule.
3. `grep -n "Departure accepted" docs/tickets/done/` — two records.

### Batching, measured
This batch cost one ticket file, one claim commit, one close commit and
one board row for four changes, against four of each had they been separate
tickets. The lifecycle's own warning holds: the saving is in the ticket
overhead and the single gate run, and the first entry saved nothing. There
are no gates here to run, so on this repository the saving is four ticket
files and eight commits.

The batch also closed early, at four entries rather than the ten-entry cap,
because the holder closes before claiming other work. That is the cost the
section names, showing up on the first use.

### Review
N/A — trivial tier.
